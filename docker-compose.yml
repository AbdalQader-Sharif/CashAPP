services:
  app:
    image: node:22
    working_dir: /workspace
    volumes:
      - ./:/workspace
    ports:
      - "5173:5173"
      - "4000:4000"
    command: sh -c "npm install && npm run prisma:migrate && npm run seed && npm run dev"
